# Core Python: mergetonic

## What it is

Python implementation of the deterministic Tonic weave merge engine.

## Install

```bash
pip install mergetonic
```

## Version source

`merge-tonic-lib/pyproject.toml`

## Repo links

- Monorepo source: `merge-tonic-lib/`
- Package README: `merge-tonic-lib/README.md`

## Known caveats

- Python and TypeScript parity is validated in CI; local parity requires both environments.
