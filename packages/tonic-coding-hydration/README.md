# @mergetonic/coding-hydration

Scaffold for retrieval / vector indexing and ast-aware ranking (see migration plan Phase 3).

- `buildEmptyRetrievalArtifact()` — valid empty `tonic-retrieval-hydration.v1` payload
- Future: `astGrepChunker`, Chroma HTTP client, `attachAstMetadata` boosts
